apiVersion: v1
name: crk-aws-kms-key-create
components:
  kms:
    type: CloudResource
    plan:
      name: crk-aws-kms-key
      version: 0.0.3
    inputs:
    - name: alias
      value: "{{ .Stack.Name }}-{{ .Stack.Instance.Name }}"
    - name: description
      value: "CRK test KMS key for {{ .Stack.Name }} / {{ .Stack.Instance.Name }}"
