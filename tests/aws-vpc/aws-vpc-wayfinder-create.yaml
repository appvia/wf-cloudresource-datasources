apiVersion: v1
name: crk-aws-vpc-create
components:
  vpc:
    type: CloudResource
    plan: crk-aws-vpc@0.0.2
    inputs:
    - name: name
      value: "crk-aws-vpc-{{ .Stack.Name }}-{{ .Stack.Instance.Name }}"
