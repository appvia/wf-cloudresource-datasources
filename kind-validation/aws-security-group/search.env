# Optionally filter by a VPC created by aws-vpc validation (if present)
# Uncomment if using the validation flow with VPC create first
# FILTER="vpc_id=$(cat ../aws-vpc/out.json | jq -r '.componentOutputs.vpc.vpc_id.value')"
